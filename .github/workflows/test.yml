name: Simple Test

on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test Step
      run: |
        echo "🚀 GitHub Actions is working!"
        echo "✅ Test successful!"
        date
